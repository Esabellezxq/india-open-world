using UnityEngine;
using System.Collections;

public class Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("zhouxiaoqi");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
